# build a function to multiply a score and a multiplier
def calculate_score(score, multiplier): # 1. Missing Colon
    print('score upcoming')
    return(score * multiplier) # 2. Indentation Fix
    
print(calculate_score(3000,2)) #3. Missing Parantheses
print(calculate_score(3200,1.5))